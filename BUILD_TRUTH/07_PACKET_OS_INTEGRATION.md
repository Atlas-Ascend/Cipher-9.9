# 07 — Packet OS Integration

Packet payloads crossing incompatible domains may invoke Cipher adapters while retaining packet_id, source schema/version, target schema/version, mapping refs, and any translation loss/warnings.

Unresolvable required semantics block or escalate rather than silently guess.