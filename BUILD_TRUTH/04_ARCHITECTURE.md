# 04 — Architecture

Components: ontology/lexicon registry, alias resolver, mapping engine, schema transformer, ambiguity/confidence layer, version manager, provenance ledger, compatibility validator.

Historical Twelve Lexicons and Cipher lineages are preserved as source-bound surfaces; missing definitions remain unresolved.